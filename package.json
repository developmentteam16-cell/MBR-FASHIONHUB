{
  "name": "mens-fashion-project",
  "version": "1.0.0",
  "private": true,
  "description": "MBR Fashion Hub e-commerce backend and frontend",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "node backend/server.js",
    "postinstall": "prisma generate --schema backend/prisma/schema.prisma",
    "db:generate": "prisma generate --schema backend/prisma/schema.prisma",
    "db:test": "node backend/testDb.js",
    "install-all": "npm install --prefix backend && npm install --prefix frontend --include=dev",
    "build": "npm install --prefix frontend --include=dev && npm install --prefix backend && npm run build --prefix frontend"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.4",
    "express-async-handler": "^1.2.0",
    "express-rate-limit": "^8.5.1",
    "helmet": "^8.1.0",
    "hpp": "^0.2.3",
    "ioredis": "^5.10.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^8.0.7",
    "prisma": "^5.22.0",
    "razorpay": "^2.9.6",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
